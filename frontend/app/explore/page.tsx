import { personalityList } from "@/lib/personalityData";

export default function ExplorePage() {
  return <section><span className="eyebrow">Personality library</span><h1>Explore all 16 types</h1><p className="pageLead">These descriptions are educational summaries. Public figures are intentionally excluded because celebrity MBTI labels are speculative.</p><div className="explorerGrid">{personalityList.map((profile) => <article id={profile.type} className={`card explorerCard theme-${profile.theme}`} key={profile.type}><span className="explorerEmoji">{profile.emoji}</span><div><span className="eyebrow">{profile.type}</span><h2>{profile.name}</h2><p>{profile.summary}</p></div><details><summary>View full profile</summary><div className="detailsGrid"><div><h3>Strengths</h3><ul>{profile.strengths.map(x=><li key={x}>{x}</li>)}</ul></div><div><h3>Growth areas</h3><ul>{profile.growthAreas.map(x=><li key={x}>{x}</li>)}</ul></div><div><h3>Careers</h3><ul>{profile.careers.map(x=><li key={x}>{x}</li>)}</ul></div></div><p><strong>Communication:</strong> {profile.communication}</p><p><strong>Work style:</strong> {profile.workStyle}</p><p><strong>Relationships:</strong> {profile.relationships}</p></details></article>)}</div></section>;
}
